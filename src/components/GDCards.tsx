import React from 'react';
import { Box, Typography } from '@mui/material';
const GDCards: React.FC = () => {
  return <Box className="p-4 text-center">
      <Typography variant="body1">
        GD Cards content will be displayed here
      </Typography>
    </Box>;
};
export default GDCards;